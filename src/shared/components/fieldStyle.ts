/**
 * 文本输入框的一套类名（条目改名、模板搜索、首页筛选）。
 * 是个常量而不是组件：调用点还要往上叠 `pl-*`（图标让位）、`!min-h-*`（首页比浮层里矮一档）。
 */
export const FIELD =
  'min-h-14 w-full rounded-xl border border-line bg-surface-2 px-3 text-base outline-none focus:border-sky-500/60 short:min-h-11'
